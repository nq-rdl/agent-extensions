## 0.18.0 - 2026-06-28
### Removed
* Remove the `jules` plugin — `dispatch`, `dispatch-creator`, and the `jules-orchestrator` agent — no longer used
### Fixed
* Skip the changelog-fragment gate and auto-triage job for Dependabot PRs so dependency-bump PRs land with green CI (Dependabot runs cannot read Actions secrets or run `changie new`)
