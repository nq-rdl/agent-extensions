## 0.24.0 - 2026-07-14
### Added
* Add a `zensical` pixi task (docs env) so contributors review docs locally with `pixi run zensical serve` / `pixi run zensical build`, no `-e docs` flag needed
* Add a dedicated Zensical Docs dev container (`.devcontainer/docs/`) so macOS contributors preview docs via `pixi run zensical serve`; pinned to linux/amd64 to match the linux-64-only docs env
