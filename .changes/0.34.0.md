## 0.34.0 - 2026-09-23
### Fixed
* Document that the changie fragment check also rejects fragments whose YAML does not parse, with a regression test for an unquoted `body:` containing `: ` (#187)
* The pre-push unit-test hook clears `GIT_DIR`, `GIT_WORK_TREE` and `GIT_INDEX_FILE` so tests that build temp git repos pass when pushing from a linked worktree
* Document the observed `main` branch protection (no required status checks on 2026-09-23), separate CI results from merge enforcement, and clarify planned link checks (#302)
* README explains `/<subject>:<skill>` invocation, updating and removing plugins, and links to CONTRIBUTING.md and the roadmap epics and project board (#56)
