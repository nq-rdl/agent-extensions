# Changelog
All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html),
and is generated by [Changie](https://github.com/miniscruff/changie).


## 0.9.0 - 2026-06-05
### Added
* New `openai-codex` marketplace auto-installs the `codex` plugin — Codex delegation and code review from Claude Code (#90)
* New `rust` plugin exposing the `rust-explain` skill as `/rust:rust-explain` — a tutor for reading, navigating, and sanity-checking unfamiliar Rust code (lifetimes, traits, `unsafe`, numeric crates). Synced from agent-skills@v0.9.0.
* Add `lucid` plugin wiring the hosted Lucid MCP server (Lucidchart / Lucidspark) over Streamable HTTP — installable standalone via `/plugin`, authenticated with OAuth (no API key)
* Skill bundles can now group members under a subject plugin via an explicit `{source, leaf}` mapping (e.g. `{source: go-gh, leaf: gh}` → `go:gh`): `sync-plugins.sh` copies the flat upstream `skills/<source>/` into `plugins/<pluginName>/skills/<leaf>/`, renaming to the leaf so Claude Code invokes `<plugin>:<leaf>`. Grouping is owned in the registry, so the upstream `agent-skills` tree stays flat. Flat string members are unchanged (verified no-diff). New `scripts/check_grouping.py` enforces the contract (valid member shape, unique pluginName, no duplicate leaf). Part of the subject-grouping migration (#102).
* New `rdl` meta-plugin: `claude plugin install rdl@rdl` installs every subject plugin in one command (it declares each as a same-marketplace dependency), and `claude plugin uninstall rdl --prune` removes the set plus orphaned dependencies. Part of #101.
* `plugin.json` and `marketplace.json` are now generated from the registry (`registry/bundles/*.yaml` + `registry/marketplace.yaml` + `VERSION`) by `scripts/generate_manifests.py`, with a `--check` CI gate that fails on drift — the structural fix for #100 metadata rot. Release versioning now flows from a single `VERSION` file.
* Generate `docs/bundles.md` from the registry and flag synced-but-unmapped skills in the sync PR body
* Publish 9 previously-unmapped skills as subject plugins: obsidian (bases/cli/markdown), argo-cd, bitwarden, defuddle, zod, conventional-commits, pre-commit
### Changed
* Updates marketplace owner and `pyproject.toml` author email to the `tsv-datalab@health.qld.gov.au` team address — replaces a personal contact
* Regroup all plugins by subject — skills invoke as `/<subject>:<facet>`; retire the swe/infra/informatics/dev-tools/meta domain bundles (#101, #102)
### Removed
* Retired the `dataops` bundle — its six skills (csv, xlsx, pdf, docx, canvas-design, duckdb) were removed upstream in agent-skills@v0.9.0 — and trimmed `dispatch`/`opencode` from `dev-tools` and `tdd`/`tdd-team-workflow` from `swe`. Reconciles the registry, marketplace, plugin trees, and docs so `validate-bundles` is green again (#100).
* Removed two stale docs pages — `docs/claude.md` (superseded by `docs/ARCHITECTURE.md`; it still described the pre-#83 symlink/submodule model) and `docs/tdd-workflow-review.md` (a review of the `tdd-team-workflow` skill retired in #100).
* Removes the stale `docs/superpowers` plan doc — the grouping mechanism it described already shipped in #108
### Fixed
* Hardened the agent-skills sync pipeline so issue #100-class drift cannot merge unnoticed. Sync PRs are now opened via a GitHub App token so `validate.yml` actually runs on them (bot-token PRs previously received zero CI); the PR body is a concise added/removed/modified summary with a drift callout instead of a 60k-line diff dump; change detection uses `git status --porcelain` so additions-only syncs are not skipped; `sync-plugins.sh` prunes stale plugin copies of upstream-removed skills; `validate-plugins.sh` now validates skill references and plugin copies; and a new `check_consistency.py` enforces registry↔marketplace↔plugins agreement. Pipeline logic is extracted into unit-tested scripts (`tests/`).
* Reconciled `lucid` manifest drift surfaced by manifest generation: the marketplace and plugin descriptions are unified to the single registry value and the stale `0.6.0` version is corrected to `0.8.0` (#100).
* Make the release workflow idempotent — tolerate a changelog batched ahead of the tag (pre-batched release PRs) instead of failing the "nothing to release" guard or double-batching (#111)

## 0.8.0 - 2026-05-30
### Removed
* Remove all Gemini CLI support after its discontinuation — the `.gemini/` extension tree, `GEMINI.md`, the `gemini-cli` skill, and the `gemini-cli-go` MCP server. The catalog now targets Claude Code exclusively.
* Removes the `pi-rpc` skill, MCP server, and prebuilt pi-server binaries — pi.dev support dropped from `dev-tools` (#97)
### Fixed
* Rewrite forced-eval-hook to emit an advisory skill catalogue via additionalContext instead of coercive prompt-injection-shaped prose, gated on use intent (#78)
* Harden the skill sync so an upstream rename or removal no longer silently breaks it — `sync-plugins.sh` now warns and skips unresolved registry references (and prunes stale plugin copies) instead of aborting before the PR is opened, `validate.yml` remains the authoritative gate, and `sync-skills.yml` opens a tracking issue on failure.

## 0.7.0 - 2026-05-14
### Removed
* Drops pi.dev publishing target (`pidev/`) — `pi-rpc` skill and MCP server remain as runtime tools
* Drops Codex publishing target — removes `.codex-plugin/` manifests, `.agents/plugins/marketplace.json`, and Codex marketplace validation
* Drops OpenCode publishing target and `opencode` skill — repo no longer publishes to OpenCode

## 0.6.0 - 2026-05-13
### Added
* New `send-pr` skill in `swe` bundle — single command to commit, push, raise a PR, and request review (#88)
* Adds reasoning effort, thinking budget, and thinking level configuration docs to `gemini-cli` skill (#88)
### Fixed
* Syncs pi-server binaries from agent-skills@v0.7.0

## 0.5.4 - 2026-05-02
### Fixed
* Vendor real-file copies of skills and agents into `plugins/<bundle>/` so `claude plugin install` produces a self-contained tree (#83). Symlinks pointing outside the plugin root no longer dangle in the per-user cache. Adds `scripts/sync-plugins.sh` for refreshing copies; the `sync-skills.yml` workflow now runs it after each upstream sync. Drops the `release` branch + `scripts/materialize.sh` infrastructure — `marketplace.json` reads directly from `./plugins/<bundle>` on the installed ref.

## 0.5.3 - 2026-05-02
### Fixed
* Plugin manifest cleanup: move swe .mcp.json to plugin root (was being silently ignored inside .claude-plugin/), collapse dev-tools duplicate .mcp.json into a single root-level file using ${CLAUDE_PLUGIN_ROOT}, drop redundant skills: ./skills/ from six manifests, and update validate-plugins.sh to expect .mcp.json at plugin root per the Claude Code plugin spec (fixes #80).
* Materialize symlinks at release time so installed plugins are self-contained (#79). The release branch is regenerated from a fresh dist/release/ tree on every tag — main keeps symlinks for development.

## 0.5.2 - 2026-04-26
### Fixed
* Sync pi-server binaries from agent-skills@v0.6.1.

## 0.5.1 - 2026-04-26
### Added
* Add `LICENSE-CC-BY-4.0` and a README license section clarifying that code is MIT and documentation/skill content is CC BY 4.0
* Document the AGENTS / CLAUDE / GEMINI file conventions in README (`## Agent File Management`) and add SPDX scope identifiers to the License section

## 0.5.0 - 2026-04-26
### Added
* Adds new top-level agents/ primitive and validator support. Introduces 30 subagents derived from github/awesome-copilot (MIT), plus .gemini/ native discovery tree. scripts/validate-plugin-hooks.sh renamed to scripts/validate-plugins.sh with agents checks; old name kept as back-compat symlink.
* swe bundle: adds 9 subagents — debug, janitor, playwright-tester, adr-generator, wg-code-alchemist, wg-code-sentinel, gem-reviewer, hlbpa, api-architect. Flips gemini target to enabled.
* infra bundle: adds 9 subagents — postgresql-dba, mongodb-performance-advisor, terraform, terraform-iac-reviewer, terratest-module-testing, platform-sre-kubernetes, github-actions-expert, se-gitops-ci-specialist, arch-linux-expert. Flips gemini target to enabled.
* dev-tools bundle: adds 7 subagents — context-architect, go-mcp-expert, repo-architect, plan, address-comments, se-technical-writer, research-technical-spike. Flips gemini target to enabled.
* meta bundle: adds 5 subagents — prompt-builder, critical-thinking, doublecheck, se-responsible-ai-code, agent-governance-reviewer. Flips gemini target to enabled.
* Add native Codex marketplace and plugin manifests for the `swe`, `infra`, `dataops`, `informatics`, `dev-tools`, and `meta` bundles
### Changed
* Document Codex packaging and install flow, and extend validation and release versioning to cover Codex plugin manifests
* Move the README Codex install walkthrough into docs/codex.md; README now links there instead of duplicating content
### Removed
* Remove docs/awesome-copilot-review.md; the point-in-time adoption review has been acted on and is superseded by the shipped agents and skills

## 0.4.0 - 2026-04-20
### Added
* Adds changie-fragment-lint PreToolUse hook — blocks fusion and missing flag, warns on word count (#40)
* Add worktrunk plugin — external reference to max-sixty/worktrunk (git worktree CLI for parallel AI agent workflows)

## 0.3.1 - 2026-04-17
### Changed
* Sync skills from agent-skills@v0.3.1 — audit-driven fixes across 26 skills for RST link rot, hook events, and metadata

## 0.3.0 - 2026-04-16
### Changed
* Sync skills from agent-skills@v0.3.0 — adds argo-cd, conventional-commits, stitch-design-md, sops, zod, obsidian, and pre-commit skills; completes RST migration for all references

## 0.2.1 - 2026-04-08
### Changed
* Skill sync now clones from release tag instead of HEAD, triggered on each agent-skills release

## 0.2.0 - 2026-04-08
### Added
* Changie changelog management and automated release workflow with version bumping
