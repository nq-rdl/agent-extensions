{
  "description": "SQL Review guardrails: a SessionStart preflight that reports whether the project is initialised (.sqlreview/) and which reviews are stale, and a PreToolUse guard that only lets review/scope JSON be written when every assumption and limitation carries a complete human-confirmation record, and keeps rendered markdown and config edits on the sanctioned path.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/sql-review-preflight.sh",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/sql-review-guard.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
